{
  "name": "tele-yt-bot",
  "description": "Telegram bot for YouTube downloading (Node.js, Telegraf, ytdl-core)",
  "buildpacks": [
    { "url": "heroku/nodejs" }
  ],
  "env": {
    "BOT_TOKEN": {
      "description": "Telegram bot token (required)",
      "required": true
    }
  },
  "formation": {
    "web": {
      "quantity": 1,
      "size": "free"
    }
  },
  "scripts": {
    "postdeploy": "echo 'tele-yt-bot deployed!'"
  }
}
